// index          -4      -3      -2      -1
// index           0       1       2       3
const colors = ['red', 'green', 'blue', 'green']

const index = colors.indexOf('green', -2)
console.log(index);
